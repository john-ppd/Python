import import_this_file

'''
To use modules from import_this_file we put a . after import_this_file and then we can use its funtions, an external file with code is called a module, you import them to use their functions, google python modules to see avaiable ones.

Inside our lib folder is where the external modules will be, might need to download from github and add using pip install python-docx

pip is a package manager, pree installed in python 3. open cmd on pc and check if pips installed.
Steps to install external python module:
1)pip --version
2)pip install python-docx
3)check "site-packages" folder, in lib folder
4)
'''
print(import_this_file.randomRoll(10))